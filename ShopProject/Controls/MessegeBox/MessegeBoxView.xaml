<Window x:Class="ShopProject.Controls.MessegeBox.MessegeBoxView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShopProject.Controls.MessegeBox"
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resource/Theme/LightTheme/ButtonStyle.xaml"/> 
            </ResourceDictionary.MergedDictionaries> 
        </ResourceDictionary>
    </Window.Resources>
    <Border Width="420"
            Padding="25"
            Background="White" 
            BorderBrush="#DDDDDD"
            BorderThickness="1">

        <Grid>

           <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <!-- ICON -->
            <Border Width="65"
                    Height="65"
                    CornerRadius="32.5"
                    HorizontalAlignment="Center"
                    Background="{Binding ColorIconBackground}">

                <TextBlock Text="{Binding Icon}"
                           FontSize="32"
                           FontWeight="Bold"
                           Foreground="{Binding ColorIcon}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>

            <!-- TITLE -->
            <TextBlock Grid.Row="1"
                       Margin="0,18,0,8"
                       Text="{Binding Title}"
                       FontSize="21"
                       FontWeight="SemiBold"
                       Foreground="#222222"
                       HorizontalAlignment="Center"/>

            <!-- MESSAGE -->
            <TextBlock Grid.Row="2"
                       Margin="10,0,10,25"
                       Text="{Binding Message}"
                       FontSize="15"
                       Foreground="#666666"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       MaxWidth="350"/>

            <!-- BUTTONS -->
            <Grid Grid.Row="3">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="CancelButton"
                        Grid.Column="0"
                        Height="42"
                        Margin="0,0,5,0"
                        Content="Скасувати"
                        Click="CancelButton_Click"
                        Visibility="Collapsed"/>

                <Button x:Name="OkButton"
                        Grid.Column="1"
                        Height="42"
                        Margin="5,0,0,0"
                        Content="OK"
                        Click="OkButton_Click"  />

            </Grid>

        </Grid>

    </Border>
</Window>
