<Page x:Class="ShopProject.View.Common.Start.StartView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ShopProject.View.Common.Start"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resource/Theme/LightTheme/ButtonStyle.xaml"/> 
                <ResourceDictionary Source="/Resource/Theme/LightTheme/TextBlockstyle.xaml"/>  
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    <Grid Background="#F3F3F3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Border Padding="25"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                CornerRadius="2"
                Background="#FFFFFF"
                BorderBrush="#DDDDDD" 
                BorderThickness="1" >
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Магазин" Background="Transparent" FontSize="20" HorizontalAlignment="Center" Style="{StaticResource GeneralTextBlock}"/>
                <Button Width="230" Height="50" FontSize="16" Command="{Binding OpenServerSelectionPageCommand}" Content="Підключитися до БД" Margin="10" Style="{StaticResource GeneralButton}"/>
                <Button Width="230" Height="50" FontSize="16" Content="Створити БД" Style="{StaticResource GeneralButton}"/>
                <Button Width="230" Height="50" FontSize="16" Command="{Binding CloseWindowCommand}" Content="Вийти" Margin="10" Style="{StaticResource ExitButton}"/> 
            </StackPanel>
        </Border>
        <TextBlock VerticalAlignment="Top" Text="v0.0.1" Opacity="0.5" HorizontalAlignment="Right" Margin="0,10,10,0"/>
    </Grid>
</Page>
